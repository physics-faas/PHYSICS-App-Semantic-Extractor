This is a folder to capture different application graphs as they are created in the PHYSICS process. It contains in most cases the example outputs of the Design Environment that are fed as inputs to the Extactor, but more importantly the outputs of the Semantic Extractor following the processing of the graph.